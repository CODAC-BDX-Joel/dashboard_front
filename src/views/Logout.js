import React from 'react';

const Logout = () => {

    //Remove access token in Authcontext inside useEffect
    return (
        <div>
            Loading...
        </div>
    );
};

export default Logout;